import styles from "./Home.module.css";

function Home() {
  return (
    <div className={styles[""]}>
      <p>home</p>
      <p>
        handy hint for devs: with the "stage" scene - control+option+i will open
        up a debug inspector. (ctrl+alt+i in windows)
      </p>
    </div>
  );
}

export default Home;
